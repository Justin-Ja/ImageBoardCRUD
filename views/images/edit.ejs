<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

<body>
    <%- include('../partials/navbar') %>
    <h1>Editing post</h1>
    <div>
        <p>Currently editing <b><%=post.username%>'s</b> post</p>
    </div>    

    <form method="POST" action="/images/<%=post.id%>?_method=PATCH">
        <section>
            <label for="textAreaDescription">Description of the post: </label> 
            <textarea name="description" id="" cols="30" rows="5" required><%= post.description  %></textarea>
        </section>
        <section>
            <label for="ImageURL">URL for the image: </label>
            <input type="url" name="img" id="" required>
            <p>The current URL being hosted is: <%=post.img%></p>
        </section>
        <button class="button-submit">Save</button>
    </form>
    
    <a href="/images"><button class="button-delete">Cancel edit</button></a>

    <%- include('../partials/footer') %>
</body>
</html>